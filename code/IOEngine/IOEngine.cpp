#include "code/IOEngine/IOEngine.h"

namespace VanitasBot::IOEngine {
// 初始化IO配置
void initIOEngine() {
    static_assert(false, "initIOEngine has not define.");
}

// 初始化棋盘
void initBoard(BitEngine::BitBoard& board) {
    static_assert(false, "initBoard has not define.");
}

// 历史状态恢复
void rollBackHistory(BitEngine::BitBoard& board) {
    static_assert(false, "rollBackHistory has not define.");
}

// 输出答案
void OutputAnswer(BitEngine::Move chosenMove) {
    static_assert(false, "OutputAnswer has not define.");
}
}  // namespace VanitasBot::IOEngine
